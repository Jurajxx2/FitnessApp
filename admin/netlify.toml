# admin/netlify.toml
[build]
  base    = "admin"
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options            = "DENY"
    X-Content-Type-Options     = "nosniff"
    Referrer-Policy            = "strict-origin-when-cross-origin"
    Permissions-Policy         = "camera=(), microphone=(), geolocation=()"
    # Admins can preview stored Supabase images, configured HTTPS image URLs,
    # and local files selected for upload (which browsers expose as blob: URLs).
    Content-Security-Policy    = "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; connect-src 'self' https://*.supabase.co; img-src 'self' data: blob: https:; frame-ancestors 'none';"

# SPA fallback — all paths serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
